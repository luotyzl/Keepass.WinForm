<Window x:Class="Keepass.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:Keepass.Wpf.Converters"
        mc:Ignorable="d"
        Title="KeePass" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e1e">
    <Window.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- Icon Button Style -->
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="Height" Value="140"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="#2d2d2e"
                                BorderThickness="1"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2d2d2e"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3d3d3e"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Settings Button in Top Right -->
        <Button Style="{StaticResource IconButtonStyle}"
                Command="{Binding SettingsCommand}"
                Width="80" Height="80"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="20"
                TabIndex="2">
            <StackPanel>
                <TextBlock Text="&#xE713;"
                          FontFamily="Segoe MDL2 Assets"
                          FontSize="32"
                          Foreground="#b7b7b8"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,5"/>
                <TextBlock Text="Settings"
                          FontSize="11"
                          Foreground="#b7b7b8"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Button>

        <!-- Centered Password Area -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="500" Width="500">
            <!-- Warning Message -->
            <Border Background="#ec655a"
                    CornerRadius="4"
                    Padding="15,10"
                    Margin="0,0,0,20"
                    Visibility="{Binding IsErrorVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE7BA;"
                              FontFamily="Segoe MDL2 Assets"
                              FontSize="16"
                              Foreground="White"
                              VerticalAlignment="Center"
                              Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding ErrorMessage}"
                              Foreground="White"
                              TextWrapping="Wrap"
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Database File Info -->
            <TextBlock Text="{Binding DatabaseFileDisplay}"
                       Foreground="#b7b7b8"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"
                       TextWrapping="Wrap"/>

            <!-- Password Input -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <PasswordBox Grid.Column="0"
                            Name="PasswordBox"
                            Height="50"
                            FontSize="18"
                            Background="#2d2d2e"
                            Foreground="#f7f7f7"
                            BorderBrush="#444445"
                            BorderThickness="1"
                            Padding="15,0"
                            PasswordChanged="PasswordBox_PasswordChanged"
                            KeyDown="PasswordBox_KeyDown"
                            TabIndex="1">
                    <PasswordBox.Style>
                        <Style TargetType="PasswordBox">
                            <Style.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#317ef6"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </PasswordBox.Style>
                </PasswordBox>

                <Button Grid.Column="1"
                        Width="50" Height="50"
                        Background="#317ef6"
                        BorderThickness="0"
                        Cursor="Hand"
                        Command="{Binding LoginCommand}"
                        TabIndex="2">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                               Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#4190f7"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#2463c8"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="&#xE974;"
                              FontFamily="Segoe MDL2 Assets"
                              FontSize="20"
                              Foreground="White"/>
                </Button>
            </Grid>

            <!-- Hint Text -->
            <TextBlock Text="Configure database in Settings"
                       Foreground="#b7b7b8"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       Margin="0,25,0,0"
                       Visibility="{Binding ShowOpenHint, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</Window>